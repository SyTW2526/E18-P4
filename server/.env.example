## Copy this file to server/.env and fill values before running docker-compose
# Connection string that the server reads from ATLAS_URI. For local docker-compose setup
# we provide a fallback in docker-compose.yml (mongodb://root:example@mongo:27017/meanStackExample?authSource=admin)
ATLAS_URI=mongodb://root:example@mongo:27017/meanStackExample?authSource=admin
ATLAS_URI="mongodb+srv://<username>:<password>@sandbox.jadwj.mongodb.net/meanStackExample?retryWrites=true&w=majority"
